
@using System.Data
@{
    DataTable dt = new DataTable();
    dt = ViewBag.ColumnNames;
    DataTable dt1 = new DataTable();
    dt1 = ViewBag.weeklyOverview;
    <link href="~/assets/plugins/bootstrap-datatable/css/dataTables.bootstrap4.min.css" rel="stylesheet" type="text/css">
    <link href="~/assets/plugins/bootstrap-datatable/css/buttons.bootstrap4.min.css" rel="stylesheet" type="text/css">
    <div class="card">
        <div class="table-responsive">
            <table class="table table-bordered" id="weeklyoverview" style="width:100%">
                <thead>
                    <tr>
                        <td rowspan="2"></td>
                        @*@for (int i = 0; i < dt.Rows.Count - 1; i++)
                            {
                                <th colspan="3" scope="colgroup" style="text-align:center;">@dt.Rows[i][0].ToString()</th>
                            }*@
                    </tr>
                    <tr>

                        <th scope="col">Min</th>
                        <th scope="col">Max</th>
                        <th scope="col">Avg.</th>

                    </tr>
                </thead>
                @for (int i = 0; i < dt1.Rows.Count; i++)
                {
                    <tr>
                        <th scope="row">@dt1.Rows[i][0].ToString()</th>

                        @for (int j = 1; j < dt1.Columns.Count; j++)
                        {
                            <td>@dt1.Rows[i][j].ToString()</td>

                        }
                    </tr>
                }
            </table>
        </div>
    </div>

}
    <script>
    $(document).ready(function () {
        //Default data table
        $('#weeklyoverview').DataTable();
    });

    </script>